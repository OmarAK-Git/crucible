diff --git a/backend/corpus.py b/backend/corpus.py
index a2d4e68..f7b1d91 100644
--- a/backend/corpus.py
+++ b/backend/corpus.py
@@ -21,3 +21,4 @@
     for uf in upload_files:
-        file_path = temp_dir / uf.filename
+        safe_filename = Path(uf.filename).name
+        file_path = temp_dir / safe_filename
         file_path.parent.mkdir(parents=True, exist_ok=True)
diff --git a/backend/gpt_adapter.py b/backend/gpt_adapter.py
index a429188..b5b8d23 100644
--- a/backend/gpt_adapter.py
+++ b/backend/gpt_adapter.py
@@ -34,3 +34,3 @@
     response = await client.chat.completions.create(
-        model="gpt-5",
+        model="gpt-4o",
         messages=[
diff --git a/backend/sessions.py b/backend/sessions.py
index b3c2a1a..d2a34b2 100644
--- a/backend/sessions.py
+++ b/backend/sessions.py
@@ -1,8 +1,8 @@
 """
-In-memory session store for Crucible Phase 1.
+In-memory session store for Crucible.
 
 NOTE: This is a temporary, in-memory dictionary-based store guarded by a threading.Lock.
-It is for Phase 1 only; SQLite-based persistence will land in Phase 3.
+It is for Phase 1 & 2; SQLite-based persistence will land in Phase 3.
 """
diff --git a/tests/test_corpus.py b/tests/test_corpus.py
new file mode 100644
index 0000000..7c5e26f
--- /dev/null
+++ b/tests/test_corpus.py
@@ -0,0 +1,38 @@
+import pytest
+import tempfile
+from pathlib import Path
+from unittest.mock import MagicMock, AsyncMock
+from fastapi import UploadFile
+from backend.corpus import extract_and_read
+
+@pytest.mark.asyncio
+async def test_path_traversal_mitigation():
+    """
+    Verifies that the corpus extraction sanitizes filenames to prevent path traversal
+    vulnerabilities (e.g. attempting to upload a file named '../../evil.txt').
+    """
+    with tempfile.TemporaryDirectory() as temp_dir:
+        temp_path = Path(temp_dir)
+        
+        # Mock an UploadFile with a malicious path traversal filename
+        mock_file = MagicMock(spec=UploadFile)
+        mock_file.filename = "../../../evil.txt"
+        mock_file.read = AsyncMock(side_effect=[b"malicious content", b""])
+        
+        scanned = await extract_and_read(temp_path, [mock_file])
+        
+        # Verify the file was written to the base name inside the temp_dir, not outside
+        expected_file_path = temp_path / "evil.txt"
+        assert expected_file_path.exists()
+        
+        # Verify that it didn't write outside the temp directory (e.g. escaping to parent directories)
+        escaped_path = temp_path.parent.parent.parent / "evil.txt"
+        if escaped_path.exists() and escaped_path.resolve() != expected_file_path.resolve():
+            # Cleanup if it somehow got created
+            try:
+                escaped_path.unlink()
+            except Exception:
+                pass
+            pytest.fail("Path traversal vulnerability: file escaped the temporary directory!")
+            
+        assert len(scanned) == 1
+        assert scanned[0]["path"] == "evil.txt"
+        assert scanned[0]["content"] == "malicious content"
